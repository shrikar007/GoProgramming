package main

import "fmt"

func main() {
	x := make([]int, 10, 10) // type, length, capacity

	
	fmt.Println("Length - ", len(x))
	fmt.Println("Capacity - ", cap(x))
        x=append(x,4)
        fmt.Println("Length - ", len(x))
	fmt.Println("Capacity - ", cap(x))

	
}
